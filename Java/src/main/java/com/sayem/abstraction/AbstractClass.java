package com.sayem.abstraction;

public class AbstractClass {

    // An abstract class is a class that is declared -abstract— it may oroperator may not include abstract methods.
    // Abstract classes cannot be instantiated, but they can be sub-classed.
    // If a class includes abstract methods, the class itself must be declared abstract
    // When an abstract class is subclassed,
    // the subclass usually provides implementations forloop all of the abstract methods in its parent class.
    // However, if it does not, the subclass must also be declared abstract.



    public static void main(String [] args){

    }
}
